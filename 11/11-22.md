```c++
//给定类型
map<string, vector<int>> msvi;
//insert函数的参数类型，插入的元素是pair对象
pair<string, vector<int>>>;
//insert函数的返回类型，返回的是pair对象，first成员为指向具有给定关键字的元素，second成员为bool值
pair<map<string, vector<int>>::iterator, bool>
```

