```c++
Base *p = &d1;		//合法
p = &d2;       		//非法，若基类的公有成员是可访问的，则派生类向基类的类型转换是可访问的，反之则不行
p = &d3;        	//非法，若基类的公有成员是可访问的，则派生类向基类的类型转换是可访问的，反之则不行
p = &dd1;      		//合法
p = &dd2;       	//非法，若基类的公有成员是可访问的，则派生类向基类的类型转换是可访问的，反之则不行
p = &dd3;    		//非法，若基类的公有成员是可访问的，则派生类向基类的类型转换是可访问的，反之则不行
```

